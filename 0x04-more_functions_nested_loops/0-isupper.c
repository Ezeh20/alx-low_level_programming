#include "main.h"

/**
 * _isupper - checks if the argument is an uppercase
 * @c: the character to be checked
 * Return: 1 is c is upper or 0 if c is lower
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
